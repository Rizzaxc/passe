import 'package:flutter/material.dart';

class LocationSubtab extends StatelessWidget {
  const LocationSubtab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Location Content'));
  }
}
