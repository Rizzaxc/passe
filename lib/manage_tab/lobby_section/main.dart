import 'package:flutter/material.dart';

class LobbySubtab extends StatelessWidget {
  const LobbySubtab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Lobby Content'));
  }
}
