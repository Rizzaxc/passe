# Admin-only Supabase credentials. Never bundled as a Flutter asset — only
# read by the local `rps schema` dev script (pubspec.yaml). Copy this file to
# secret/db.env and fill in real values; secret/ is git-ignored (/secret/*).
SUPABASE_SECRET_KEY=
SUPABASE_DB_PASSWORD=
